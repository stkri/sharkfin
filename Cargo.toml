[workspace]
resolver = "3"

members = [
    "drivers/driver_traits",
    "drivers/gpio/*",
    "sharkfin_kernel",
    "kernel_utils"
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
