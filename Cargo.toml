[workspace]
resolver = "3"

members = [
    "drivers/driver_traits",
    "drivers/gpio/*",
    "drivers/uart/*",
    "sharkfin_kernel",
    "kernel_utils"
, "devices/rpi_3b_plus"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
